import React from "react";

import * as Styled from "./styles";

const Header = () => (
  <Styled.Header>
    <Styled.HeaderTitle>Bookmarks</Styled.HeaderTitle>
  </Styled.Header>
);

export default Header;
